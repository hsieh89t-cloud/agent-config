# 任務：OpenClaw 每日情報監控（測試版）

這是測試版本，只執行一個搜尋來驗證流程。

## 核心使命
驗證每日監控任務的執行流程。

## 不可違反的AXIOM鐵則
1. **觸發規則**：立即執行，無需等待。
2. **核心搜索目標**：
   - 目標1：DeepSeek API 官方最新優惠（僅搜尋一個 Query）。
3. **信息準則**：僅保留搜索前7天內的官方發布內容。
4. **工具限制**：僅可使用web通用搜索工具。

## 標準執行流程
1. **觸發前檢查**：略過。
2. **固定搜索Query組合**（只執行一個）：
   - "DeepSeek API 最新優惠 官方公告 限時活動"
   - 搜尋結果限制為 2 條即可。
3. **去噪流水線**（簡化）：
   - 將搜尋內容壓縮至 50–100 字摘要，保留「時間、發布主體、核心內容」。
4. **輸出與歸檔**：
   - **歸檔路徑**：`/home/hsieh89t/.openclaw/workspace/01_自動化監控/OpenClaw-DeepSeek每日日誌/`
   - **文件名**：`OpenClaw-DeepSeek_每日監控日誌_TEST.md`
   - **輸出格式**：使用以下模板：

```
---
tags: [OpenClaw監控, DeepSeek API, 測試]
date: 2026-03-01
timezone: UTC+8
---

# OpenClaw-DeepSeek 每日監控日誌 TEST

## 🔍 執行概況
- 執行時間：現在
- 有效信息條數：X條
- 認知負載等級：TIER_0

## 📌 核心摘要（測試）
（50-100字摘要）

## 🎯 分模組詳細信息
### 1. DeepSeek/大模型API最新優惠
（列出找到的資訊）

## ⚠️ 待驗證/異常信息
無
```

5. **推送至Telegram頻道**（測試）：
   - 將核心摘要發送到 Telegram 頻道 `-1003711803841`。
   - 使用 `exec` 工具執行：
     ```
     cd /home/hsieh89t/.openclaw/workspace && python3 telegram_send.py --chat-id -1003711803841 "測試摘要：每日監控流程驗證完成。"
     ```

## 立即執行
現在請開始執行上述流程。完成後回報「測試完成」。